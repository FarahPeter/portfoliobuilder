/* ============================================================
   FolioForge site configuration — EDIT THIS ONE FILE
   ============================================================
   Set appUrl to wherever your client app (the Docker container
   on your NAS) is reachable, e.g. "https://app.folioforge.me".
   Every Login / Get started button on the static site uses it.
   ============================================================ */
window.FF_CONFIG = {
  appUrl: "https://build.peterfarah.com",
};
